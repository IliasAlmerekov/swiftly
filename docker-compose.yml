services:
  app:
    image: node:20-bookworm
    working_dir: /app
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      NODE_ENV: test
    command: sleep infinity

volumes:
  node_modules:
